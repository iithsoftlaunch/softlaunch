Drop the Señorita audio file here, named exactly:  senorita.mp3
(full path: public/music/senorita.mp3)

The couples wall plays this file if present — a source we own, so it can never be
taken down or swapped. If the file is missing, the wall falls back to a YouTube
stream automatically. .mp3 (or .m4a renamed to .mp3) works fine.
